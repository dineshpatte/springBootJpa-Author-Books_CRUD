server:
  port: 8080

spring:
  application:
    name: demo

  spring:
    datasource:
      url: jdbc:mysql://mysql-dinesh-pattemdinesh-079a.l.aivencloud.com:10031/defaultdb?verifyServerCertificate=true&useSSL=true&requireSSL=true&clientCertificateKeyStoreUrl=file:/path/to/keystore.p12&clientCertificateKeyStorePassword=your_keystore_password&trustCertificateKeyStoreUrl=file:/path/to/truststore.p12&trustCertificateKeyStorePassword=your_truststore_password
      username: avnadmin
      password: AVNS_R1D7uWMJ_6e4EwLiyRg
      driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always

  h2:
    console:
      enabled: true
      path: /h2-console

# Optional: define H2 datasource for testing (overrides MySQL if active profile is h2)
#spring:
#  datasource:
#    url: jdbc:h2:mem:testdb
#    driver-class-name: org.h2.Driver
#    username: sa
#    password:

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
    org.hibernate.tool.schema: DEBUG
